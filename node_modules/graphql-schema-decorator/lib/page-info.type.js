"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const graphql = require("graphql");
class PageInfo {
    constructor(count, offset, limit) {
        count = count || 0;
        offset = offset || 0;
        limit = limit > 0 ? limit : count;
        this.hasNextPage = count > (offset + limit);
        this.hasPreviousPage = offset > 0;
    }
}
exports.PageInfo = PageInfo;
exports.PageInfoType = new graphql.GraphQLObjectType({
    name: 'PageInfo',
    description: 'Contains current paging information.',
    fields: function () {
        return {
            hasNextPage: {
                type: new graphql.GraphQLNonNull(graphql.GraphQLBoolean),
            },
            hasPreviousPage: {
                type: new graphql.GraphQLNonNull(graphql.GraphQLBoolean),
            },
        };
    },
});
//# sourceMappingURL=page-info.type.js.map