import { PageInfo } from './page-info.type';
import { OrderByItem } from './order-by-item';
export declare class PaginationResponse<T> {
    private readonly count;
    private readonly nodes;
    private readonly pageInfo;
    constructor(count: number, nodes: T[], pageInfo: PageInfo);
}
export declare class PaginationRequest {
    readonly offset: number;
    readonly limit: number;
    readonly orderBy: OrderByItem[];
    constructor(offset: number, limit: number, orderBy: OrderByItem[]);
}
export declare class PaginationType {
    static build(name: string, type: any): any;
}
